export function Ads_2(props) {

    return (

        <section>

            <section>

                <img className="w-full" src={props.cover} alt="" />

            </section>

        </section>

    );

}